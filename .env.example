# Backend (Render)
PORT=3001
NODE_ENV=development
JWT_SECRET=changez-ce-secret-en-production
DB_PATH=./society.db
CORS_ORIGIN=http://localhost:5173

# Frontend (Vite) — copier dans .env.local pour dev
# VITE_API_URL=https://society-api.onrender.com/api
